import React from 'react';
import ReactDOM from 'react-dom/client';
import WinkStore from './WinkStore';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <WinkStore />
  </React.StrictMode>
);
